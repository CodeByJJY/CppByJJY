#include <iostream>

using namespace std;

int main() {
	/*
	+ : 덧셈
	- : 뺄셈
	* : 곱셈
	 / : 나눗셈. 두 개의 피연산자가 모두 정수이면 결과값은 '몫'이다.
	% : 나머지 구하기. 부동소수점(소수점)으로 표현하는 실수에서는 사용할 수 없다.
	*/

	int a = 10;
	int b = 3;
	int x = 5;

	int c = a + b;
	int d = a - b;
	int e = a * b;
	int f = a / b;
	int g = a % b;

	cout << c << "  " << d << "  " << e << "  " << f << "  " << g << endl;

	// 복잡한 산술 연산 : 일반적인 대수 계산 법칙을 따른다.

	int multiple = a / b * x;
	cout << multiple << endl;

	// 피연산자 중 하나가 실수형으로 선언되면, 결과는 실수형으로 출력된다.
	float a1 = 9.0;
	int a2 = 2;

	cout << a1 / a2 << endl;









	/*
	* auto : 초기화 값의 자료형에 따라, 변수의 자료형을 자동으로 결정한다.
	* 단, 그렇게 좋은 코드 스타일은 아니다.
	*/

	auto n = 100;				// int
	auto t = 1.5;					// float
	auto l = 1.32e12L;		// long long

	return 0;
}
